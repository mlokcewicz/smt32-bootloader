BOOT_START = 0x08000000;
BOOT_SIZE  = 32K;
APP_START  = BOOT_START + BOOT_SIZE;
APP_SIZE   = 1024K - BOOT_SIZE;

PROVIDE(__boot_start__ = BOOT_START);
PROVIDE(__boot_start__ = BOOT_START);
PROVIDE(__app_start__ = APP_START);
PROVIDE(__app_size__ = APP_SIZE);